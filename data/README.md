# Data

`sample/` contains a tiny packaged dataset for testing the demo structure. For Sol Levante or another dataset, use a config JSON pointing to the real files and processed frame/panel directories.
