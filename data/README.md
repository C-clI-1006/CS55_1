# Data

`sample/` contains tiny synthetic media only for smoke-testing that the package is wired correctly.

The full Sol Levante dataset is **not included** in this archive. Configure its local paths in `config.sollevante.json` when demonstrating the real project result.
